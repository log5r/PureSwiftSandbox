{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "swiftc_build_d",
            "command": "swiftc",
            "group": "build",
            "args": [
                "-g", "${workspaceFolder}/main.swift",
                "-o", "${workspaceFolder}/main.o"
            ],
            "problemMatcher": [],
            "detail": "Swiftc build for debug run"
        }
    ]
}